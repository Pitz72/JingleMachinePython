# Changelog v1.2.0 - Core Audio Pro & Visual Workflow

## 🚀 Nuove Funzionalità (Feature)

### Phase 2: Core Audio Pro
*   **Advanced Audio Routing:**
    *   Implementata la selezione del dispositivo di output audio (Scheda Audio) tramite `GlobalSettingsDialog`.
    *   Utilizzo di `AudioContext.setSinkId` (supportato in Electron/Chrome) per indirizzare l'audio.
    *   Nuovo hook `useAudioDevices` per elencare i dispositivi disponibili.
*   **Native Drag & Drop:**
    *   **Electron:** Supporto per il trascinamento di file audio direttamente sui pulsanti. Viene salvato il percorso assoluto (`filePath`) per lo streaming diretto dal disco, migliorando le performance e riducendo l'uso della memoria.
    *   **Web:** Fallback automatico su `FileReader` (Data URL) per la compatibilità via browser.

### Phase 3: Visual & Workflow
*   **Waveform Visualization:**
    *   Generazione automatica della forma d'onda (waveform) al caricamento del file.
    *   Utilizzo di un **Web Worker** (`waveformWorker.ts`) per l'analisi audio in background, evitando blocchi dell'interfaccia utente.
    *   Rendering SVG della waveform direttamente sul pulsante `JingleButton`, con mirroring e scaling dell'ampiezza.
*   **Auto-Ducking (Talkover):**
    *   Nuova opzione **"Talkover / Mic"** nelle impostazioni di ogni pulsante.
    *   Quando un canale "Talkover" è attivo, il volume di tutti gli altri canali non-talkover viene automaticamente attenuato (ducking) a -14dB (0.2x).
    *   Ideale per l'uso con microfoni o jingle prioritari su basi musicali.

## 🐛 Bug Fixes & Refactoring
*   **Audio Engine Stability:**
    *   Risolto un problema critico di corruzione nel file `useAudioEngine.ts` che causava errori di sintassi e runtime.
    *   Ripristinata la logica corretta per la gestione del ciclo di vita dei nodi audio e dei timer di fade.
*   **UI Fixes:**
    *   Corretto un errore di sintassi JSX in `SettingsDialog.tsx` (tag di chiusura `</div>` in eccesso) che impediva la compilazione.
*   **Type Safety:**
    *   Aggiornati i tipi TypeScript (`ButtonConfig`, `JingleGridProps`) per supportare le nuove proprietà `waveform`, `filePath` e `isTalkover`.

## 📝 Note Tecniche
*   Aggiornato `package.json` alla versione `1.2.0`.
*   Il sistema ora utilizza un'architettura ibrida per la gestione dei file: Streaming diretto per Electron (Pro) e Blob/DataURL per Web (Compatibilità).
