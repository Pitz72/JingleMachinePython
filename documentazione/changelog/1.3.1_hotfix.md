# v1.3.1 Hotfix (30/11/2025)

## 🐛 Bug Fixes
- **Correzione Crash Critico (React Error #310):** Risolto un crash in produzione causato da una violazione delle "Rules of Hooks" in `App.tsx`.
    - L'hook `handleFileDrop` era definito dopo un return condizionale, causando l'errore "Rendered more hooks than during the previous render" quando si passava dalla schermata di Benvenuto all'App principale.
    - La definizione dell'hook è stata spostata al livello superiore del componente per garantire la stabilità.
- **Stabilità Database:** Aggiornato `db.ts` per utilizzare il pacchetto locale `idb` invece di un'importazione remota `esm.sh`, garantendo il funzionamento offline e risolvendo i fallimenti nell'ambiente di test.

## 🧪 Testing
- Aggiunto un caso di test di riproduzione completo in `App.test.tsx` utilizzando Vitest e JSDOM.
- Verificato che l'applicazione esegua il rendering e gestisca le interazioni (click sui pulsanti) senza crash.
